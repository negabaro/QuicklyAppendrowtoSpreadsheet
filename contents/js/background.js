/*
chrome.runtime.onMessage.addListener(function(message, sender, sendResponse){
  if(message === "setting.searchconsole.isDisplayNone") {
    if(localStorage.getItem('setting.searchconsole.isDisplayNone') === 'true') {
      sendResponse(true);
    } else {
      sendResponse(false);
    }
  }
})
*/